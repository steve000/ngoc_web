import * as C from "../../../constants/api";
import * as AC from "../../../constants/actions";
import Api, { createAction } from "../../../utils/api";
export default {
    //商品管理_计量管理
    // 增加
    manageUnitAdd: createAction(C.MANAGE_UNIT_ADD, Api.manageUnitAdd),
    // 修改状态
    manageUnitChangeStatus: createAction(C.MANAGE_UNIT_CHANGESTATUS, Api.manageUnitChangeStatus),
    // 分页
    manageUnitList: createAction(C.MANAGE_UNIT_LIST, Api.manageUnitList),
    // 所有
    manageUnitAll: createAction(C.MANAGE_UNIT_ALL, Api.manageUnitAll),
}